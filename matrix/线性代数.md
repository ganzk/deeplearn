# 线性代数



## 向量基础

### 向量的来源



### 向量的表示



### 向量加法和数乘



### 向量的点乘



## 消元法

### 方程组



### 高斯消元法



### 高斯-诺尔当消元法

reef



## 向量空间





向量空间

列空间

零空间

矩阵的秩  跟解的关系

齐次方程 非齐次方程

特解

线性相关  基 维度



四个基本子空间



特殊矩阵回归

正交矩阵

对称矩阵

对角矩阵

上三角矩阵

下三角矩阵

旋转矩阵


## 行列式


## 特征值


### 对称矩阵
对称矩阵是指以主对角线为对称轴，各元素对应相等的矩阵。**即：A = Aᵀ**

**对称矩阵的性质**
1.实对称矩阵的所有​**​特征值都是实数​**​
2.特征向量正交

> [!NOTE] 证明：实对称矩阵的特征值都是实数
> 

因为特征向量的相互正交，所以特征向量线性无关，所以A=SΛS⁻¹，因为S是正交矩阵，所以A=QΛQᐪ，所以
$$\\
\begin{aligned}
A&=QΛQ^T\\

&=\begin{bmatrix}q_₁ & q_₂ & \cdots & q_ₙ\end{bmatrix}
\left[\begin{matrix}
 λ₁      & 0      & \cdots & 0      \\
 0      & λ₂      & \cdots & 0      \\
 \vdots & \vdots & \ddots & \vdots \\
 0      & 0      & \cdots & λₙ      \\
\end{matrix}\right]
\begin{bmatrix}q_₁^T \\ q_₂^T\\ \vdots \\ q_ₙ^T\end{bmatrix}\\

&=λ₁q_₁q_₁^T + λ₂q_₂q_₂^T + \cdots + λₙq_ₙq_ₙ^T

\end{aligned}$$

q₁q₁ᐪ是一个投影矩阵(rank=1)，所以每个对称矩阵可以分解为n多个互相垂直的投影矩阵! 或者说对称矩阵可以看成n个互相垂直的投影矩阵的线性组合。

> [!NOTE] q₁q₁ᐪ为什么是投影矩阵
> 向量的投影矩阵是：$P = (a a^T)/(a^T a)$
> 因为q是单位向量，也就是点积是1，即q₁ᐪq₁=1，所以q₁q₁ᐪ是投影矩阵

**如何判断特征值正负** #特征值正负判断
对于一些很大的矩阵，我们很难具体求出他的每个特征值，但是特征值的符号对我们很有意义，所以虽然很难求特征值，但是还是想知道特征值们的符号
**对称矩阵中主元的正负 = 特征值的正负**


### 复数矩阵

上面的对阵矩阵的性质，针对的是实对称矩阵，我们来推广到复数空间中；
首先对于向量点积的定义是：|a|² = $a^Ta$

> [!NOTE] 向量的点积
> 向量的点积，也称为​**​数量积​**​或​**​内积​**​，是一种将两个向量映射到一个标量的运算。

而对于复向量的点积是：$\overline{a}^Ta$

#### 共轭转置
共轭转置是将复矩阵先取共轭，然后再进行转置，就是$\overline{Z}^T$，又记作：$Z^H$或者$Z^*$
性质：$(AB)^H=A^HB^H$

> [!NOTE] 证明$(AB)^H=A^HB^H$
> 


#### 埃尔米特矩阵
如果一个复数矩阵 ​**​H​**​ 满足等于其共轭转置的条件，即$H^*=H$，那么它就是一个​**​埃尔米特矩阵​**​（或称自伴矩阵）。
- 埃尔米特矩阵的​**​主对角线上的元素必须是实数​**​（因为要求 $\overline{a}_{ii} = a_{ii}$）。
    
- 非对角线上的元素关于主对角线共轭对称，即 $\overline{a}_{ij} = a_{ij}$。
    
- 埃尔米特矩阵是实数领域中对称矩阵（$A^T=A$）在复数领域的推广。
所以跟实数领域的对称矩阵的性质一致，特征值都是实数，特征向量相互正交


#### 酉矩阵

如果一个复数矩阵 ​**​U​**​ 满足 $U^∗U=UU^∗=I$（其中 ​**​I​**​ 是单位矩阵），那么它就是一个​**​酉矩阵​**​。

- 酉矩阵是实数领域中正交矩阵（$Q^TQ=I$）在复数领域的推广。
    
- 酉矩阵的列（或行）向量构成一组标准正交基。


#### 傅里叶变换 
#傅里叶变换


### 正定矩阵

#### 定义

通俗定义：**正定矩阵​**​是所有特征值都为正数的​**​实对称矩阵​**​。
正式定义：一个 `n×n`的​**​实对称矩阵​**​ `A`被称为​**​正定矩阵​**​，如果它满足以下​**​等价条件之一​**​：
1.  ​**​标量条件​**​：对于​**​所有​**​的非零实向量 ​**​x​**​ ∈ ℝⁿ，都有 ​**​x​**​ᵀA​**​x​**​ > 0。
    
2. ​**​特征值条件​**​：矩阵 `A`的​**​所有​**​特征值都是正数。
	
3. ​**​主子式条件​**​：矩阵 `A`的​**​所有顺序主子式​**​（Leading Principal Minors）都大于零。


> [!NOTE]  ​为什么正定矩阵，$x^TAx > 0$
> 证明：因为A是对称矩阵$A = QΛQ^T$，所以$x^TAx = x^TQΛQ^Tx$
> 令$y=xQ^T$，所以$$ \begin{aligned}
> x^TAx &= x^TQΛQ^Tx\\ 
> &= y^TΛy\\
> &= \begin{bmatrix} y₁ & y₂ & \cdots & yₙ \end{bmatrix} × 
> \begin{bmatrix}
> λ₁ & 0 & \cdots & 0 \\
> 0 & λ₂ & \cdots & 0 \\
> \vdots & \vdots & \ddots & \vdots \\
> 0 & 0 & \cdots & λₙ
> \end{bmatrix}
> ×
> \begin{bmatrix}
> y₁ \\
> y₂ \\
> \vdots \\
> yₙ
> \end{bmatrix}\\
> &= λ₁y₁² + λ₂y₂² + ... + λₙyₙ²
> \end{aligned}$$
> 因为是正定矩阵，所以λ>0，所以$x^TAx > 0$



**正定矩阵性质**
- ​**​可逆性​**​：它是可逆的，并且它的逆矩阵 `A⁻¹`也是正定矩阵。
    
- ​**​相关性​**​：它与单位矩阵 `I`“相似”。更准确地说，存在可逆矩阵 `P`，使得 `A = PᵀP`（这个分解叫做柯西分解）。
    
- ​**​唯一平方根​**​：存在唯一的正定矩阵 `B`，使得 `A = B²`。我们可以记作 `B = A^(1/2)`。
    
- ​**​主元正性​**​：在进行LU分解（高斯消元）时，其所有主元（pivots）都是正的。这提供了另一种判断方法。
	
- ​**​正定组合​**​：如果 `A`和 `B`都是正定矩阵，那么它们的和 `A+B`也是正定的。（但它们的积 `AB`不一定是对称的，除非 `A`和 `B`可交换）。

负定矩阵
不定矩阵

顺序主子式   奇数阶主子式 < 0，偶数阶主子式 > 0

**半正定矩阵**

**负正定矩阵**

**不定矩阵**

#### 二次型

#### 最小值

主轴定理

#### 练习



---
一些资料
https://zhuanlan.zhihu.com/p/407010460


### 相似矩阵





## 奇异值分解（SVD）

#奇异值分解

### 奇异值分解

对于任意矩阵A可以分解为四个基本子空间，即
#### Ax=b的几何意义

https://zhuanlan.zhihu.com/p/34056351

#### 定义
对于**任意实数矩阵A**，他的奇异值分解是：
$$
A = UΣV^T
$$
其中U和V是正交矩阵，Σ是对角矩阵，当A是对称矩阵时奇异值分解：$A=QΛQ^T$，这时候U=V=Q，Σ=Λ，但是对于其他矩阵$A = SΛS^{-1}$则不是奇异值分解，因为S不是正交矩阵。
对于复数矩阵来说，也符合奇异值分解，只不过需要将转置，变成共轭转置：
$$
A = UΣV^*
$$

现在来求解这个方程
**V的计算**
通过$AᵀA$求解V：
$$
A^TA = VΣ^TU^TUΣV^T = VΣ^TΣV^T
$$
因为U是对角矩阵，$U^TU = I$，而$Σ^TΣ=Σ^2$(Σ是对角矩阵)是一个对角元为$σ_i^2$​的对角矩阵，所以：
$$
A^TA = VΣ^2V^T
$$
因为$A^TA$是对称矩阵，所以V就是$A^TA$的**单位特征向量**，Σ^2 就是$A^TA$特征值的对角矩阵

**U的计算**
同理可以通过$AAᵀ$求解V：
$$
AA^T = UΣ^2U^T
$$
$AAᵀ$也是对称矩阵，所以


> [!NOTE] 为什么$AᵀA$和$AAᵀ$是对称矩阵？
> 因为$$(A^TA)^T = A^TA; (AA^T)^T = AA^T$$



#### 四个基本子空间联系

**再看$A = UΣV^*$**
取其中的一个



#### 练习



## 线性变换

#### 线性变换


#### 向量的几何意义


### 基变换





