

### 卷积层

#### 卷积层介绍

在介绍卷积层之前，我们先看一下之前的神经网络有什么问题，之前的网络神经问题在于输入层，输入层是一维数据，会将数据给拉平了，比如图片这些数据，有三维数据，在使用之前神经网络，需要先把数据处理成一维数据作为输入数据，这样的话会忽略输入数据的形状，丢失数据的空间结构。

一维数据有什么问题？
1.参数爆炸，比如图像时1通道，28x28像素形状，拉成一维数据，就是784个输入数据，如果神经元有1000个，那么全连接层的参数数量为「784×1000 + 1000（偏置）≈ 78.5 万」
2.丢失空间/序列结构，图像、语音等数据的核心价值在于「局部关联性」（如图像的边缘由相邻像素构成、语音的音调由连续帧构成），但全连接层将输入拉平为一维向量后，**完全破坏了这种空间 / 序列关系**。
3.缺乏平移不变性，「平移不变性」指：同一特征（如猫的耳朵、图像的边缘）无论出现在输入的哪个位置，模型都能识别。像素位置轻微移动（如猫的图像向右移 1 个像素），拉平后的一维向量会完全变化，导致模型认为是「全新数据」，需要重新学习，泛化能力极差。

所以出现了卷积层，卷积层的输入输出称为**特征图**，其中卷积层的输入数据称为**输入特征图**，输出数据称为**输出特征图**


#### 卷积运算

**输入数据的格式**

在最开始的时候，我们都是基于图片，单通道的输入数据进行计算，比如下图

用一个“**过滤器**”或“**卷积核**”的小窗口（比如3x3或5x5的方块），在输入图像上从左到右、从上到下地滑动。通过**点乘求和**，求出一个**特征图**，如下图：

![|350](assets/Pasted%20image%2020251124165302.png)
将各个位置上滤波器的元素和输入的对应元素相乘，然后再求和。然后，将这个结果保存到输出的对应位置。将这个过程在所有位置都进行一遍，就可以得到卷积运算的输出。

在全连接的神经网络中，除了权重参数，还存在偏置。CNN中，滤波器的参数就对应之前的权重。并且，CNN中也存在偏置。这个值会被加到应用了滤波器的所有元素上。
![](assets/Pasted%20image%2020251124165457.png)


> [!NOTE] 普遍的输入数据
> 普遍的输入数据是高维数据，我们之前讨论的图片是1个通道，那么如果是多个通道呢，比如m个通道，那么输入数据就是`n x n x m`，如果含有其他参数，那么就会是更高维数据。
> 
> 如何工作？
> 对于一个 `n x n x m` 的输入，一个卷积核的尺寸不会是 `3 x 3`，而是 `3 x 3 x m`。


#### 填充

在进行卷积层的处理之前，有时要向输入数据的周围填入固定的数据（比如0等），这称为填充（padding），是卷积运算中经常会用到的处理。比如，对大小为(4,4)的输入数据应用了幅度为1的填充。“幅度为1的填充”是指用幅度为1像素的0填充周围。如下图

![](assets/Pasted%20image%2020251124165729.png)

上图中，通过填充，大小为(4,4)的输入数据变成了(6,6)的形状。然后，应用大小为(3,3)的滤波器，生成了大小为(4,4)的输出数据。这个例子中将填充设成了1，不过填充的值也可以设置成2、3等任意的整数。如果将填充设为2，则输入数据的大小变为(8,8)；如果将填充设为3，则大小变为(10,10)。


> [!NOTE] 填充的目的
> 
> **填充最主要的目的就是为了在卷积过程中保持输入特征图的空间尺寸（高度和宽度）不变。**
> 
> 填充会改变输出的大小。比如，对大小为(4,4)的输入数据应用(3,3)的滤波器时，输出大小变为(2,2)，相当于输出大小比输入大小缩小了2个元素。进行多次卷积运算的神经网络，如果每次进行卷积运算都会缩小空间，那么在某个时刻输出大小就有可能变为1，在刚才的例子中，将填充的幅度设为1，那么相对于输入大小(4,4)，输出大小也保持为原来的(4,4)。因此，卷积运算就可以在保持空间大小不变的情况下将数据传给下一层。
> 
> **保留边缘信息**
> 
> 在没有填充的情况下，输入图像边缘的像素被卷积核“看到”的次数远少于中心的像素。
> - 一个3x3的卷积核，在5x5的图像上滑动（无填充）。
>- **角落的像素**（如位置[0,0]）只参与**1次**卷积计算。
>- **中心的像素**（如位置[2,2]）会参与**9次**卷积计算。
> 
> 这意味着，网络对图像边缘信息的学习会远弱于中心信息。边缘、角落的特征可能在网络深层完全丢失。
> **填充的作用**：通过填充（特别是零填充），边缘像素也被包围在一个“上下文”中，使得它们能够和中心像素一样，平等地参与到多次卷积计算中，从而让网络能更好地学习到图像边缘的特征。


#### 步幅

应用滤波器的位置间隔称为步幅（stride）。之前的例子中步幅都是1，如果将步幅设为2，如下图所示，应用滤波器的窗口的间隔变为2个元素。

![](assets/Pasted%20image%2020251124171303.png)

对上图来说，对输入大小为(7,7)的数据，以步幅2应用了滤波器。通过将步幅设为2，输出大小变为(3,3)。

增大步幅后，输出大小会变小。而增大填充后，输出大小会变大。假设输入大小为(H,W)，滤波器大小为(FH,FW)，输出大小为(OH,OW)，填充为P，步幅为S。此时，输出大小可通过下面公式进行计算：
$$ \begin{aligned}
OH = \frac{H + 2P- FH}{S} + 1 \\ 
OW = \frac{W + 2P- FW}{S} + 1 
\end{aligned}$$
比如：
输入大小：(4,4)；填充：1；步幅：1；滤波器大小：(3,3)
$$ \begin{aligned}
OH = \frac{4 + 2*1- 3}{S} + 1 = 4\\ 
OW = \frac{4 + 2*1*- 3}{S} + 1 = 4
\end{aligned}$$

输入大小：(7,7)；填充：0；步幅：2；滤波器大小：(3,3)
$$ \begin{aligned}
OH = \frac{7 + 2*0- 3}{S} + 1 = 3\\ 
OW = \frac{7 + 2*0*- 3}{S} + 1 = 3
\end{aligned}$$

#### 3维数据的卷积运算

之前我们讨论的都是单通道的数据，如果有3个通道的数据如何计算，下图是3通道的计算过程：
![](assets/Pasted%20image%2020251124173049.png)

可以发现纵深方向（通道方向）上特征图增加了。通道方向上有多个特征图时，会按通道进行输入数据和滤波器的卷积运算，并将结果相加，从而得到输出。计算过程如下：

![](assets/Pasted%20image%2020251125154323.png)

需要注意的是输入数据和滤波器的通道数要设为相同的值。

上面的例子里生成的特征图是单通道的，因为我们使用的是一个卷积核，如果生成多个通道，那么就要使用多个卷积核，**也就是说卷积核的数量就是输出的通道数**


### 池化层

池化层跟据卷积层类似，不过池化层是用来缩小特征图的尺寸，池化层也是定义一个滑块，来取滑块内数据的最大值或者平均值，分别叫最大池化和平均池化，如下图：

![](assets/Pasted%20image%2020251127164636.png)

为什么会有池化层？
- **减小计算量和内存消耗**
	这是最实际的好处。通过减小特征图的尺寸，后续所有层需要处理的数据量都**急剧减少**。
- **防止过拟合**
	通过降低特征图的维度，池化层也间接地起到了**正则化**的作用，有助于防止模型过拟合训练数据。更少的参数意味着模型更不容易记住训练数据的噪声和细节，从而迫使它学习更鲁棒、更通用的特征。
- **获得平移、旋转和尺度不变性**
	如果图像中的目标物体**移动了几个像素**，最大池化仍然很可能捕捉到同一个最强的激活值。这意味着网络不再过分关心特征的**精确位置**，而更关心它**是否存在**。
	如下图，偏移了一个位置也不会发生变化
	![](assets/Pasted%20image%2020251127170156.png)
- **扩大感受野**
	在池化之后，后续层中一个像素点的感受野（对应原始图像的区域）会变得更大。这使得深层网络能够用更少的层数就整合更大范围的上下文信息。

池化层通过这种**下采样**，保持特征不变性，卷积层，是主要为了特征提取，池化层没有卷积核的权重，是个固定的操作，取最大值或者取平均值，通道数也不会发生变化，将信息浓缩。将这个地方有特征，变成这块区域有特征。


### 卷积层实现


#### 高维数据实现

我们现在有10个高为28、长为28、通道为1的数据。我们可以用4维数据表示，即（10,1,28,28）,我们用代码生成这些数据：
```python
x = np.random.randn(10,1,28,28)  
print(x.shape) # 四维数据  
print(x[0].shape) # 三维数据  
print(x[0,0].shape) # 二维数据  
print(x[0,0,0].shape) # 一维数据

-------------------------------------------------
(10, 1, 28, 28)
(1, 28, 28)
(28, 28)
(28,)
```


#### `im2col`函数

`im2col` 是一种在卷积神经网络（CNN）中常用的**数据重排技术**，它的作用是将图像中的局部感受野（卷积窗口）**展开成列**，从而将卷积运算转化为矩阵乘法，便于利用高效的线性代数库进行加速计算。

卷积核每次滑动时，将输入数据转化为二维数据，同时卷积核也转换成二维数据，这样就可以进行矩阵乘法了，核心就是将多维数据转为二维数据。如下图

![|475](assets/whiteboard_exported_image%20(1)%201.png)


> [!NOTE] "行优先"和"列优先"
> 在上图出现了“行优先”，行优先就是将每个窗口的数据变成一行数据，而列数据相反，就是将每个窗口的数据变为列数据，如果是多批数据的话，行优先的话就是在数据下方增加，而列优先的话就是在右侧增加
>行优先：
>列优先



**代码实现**
```python
def im2col(input_data, filter_h, filter_w, stride=1, pad=0):  
    """  
  
    Parameters    ----------    input_data : 由(数据量, 通道, 高, 长)的4维数组构成的输入数据  
    filter_h : 滤波器的高  
    filter_w : 滤波器的长  
    stride : 步幅  
    pad : 填充  
  
    Returns    -------    col : 2维数组  
    """    N, C, H, W = input_data.shape  
    out_h = (H + 2*pad - filter_h)//stride + 1  
    out_w = (W + 2*pad - filter_w)//stride + 1  
  
    img = np.pad(input_data, [(0,0), (0,0), (pad, pad), (pad, pad)], 'constant')  
    col = np.zeros((N, C, filter_h, filter_w, out_h, out_w))  
  
    for y in range(filter_h):  
        y_max = y + stride*out_h  
        for x in range(filter_w):  
            x_max = x + stride*out_w  
            col[:, :, y, x, :, :] = img[:, :, y:y_max:stride, x:x_max:stride]  
  
    col = col.transpose(0, 4, 5, 1, 2, 3).reshape(N*out_h*out_w, -1)  
    return col  
  
x2 = np.random.rand(2, 2, 4, 4)  
print(x2)  
inCol = im2col(x2,2,2,2,0)  
print(inCol.shape)  
print(inCol)

-----------------------------------------------------------------
[[[[26 11 22 30]
   [27  9 21 16]
   [27  3 28  3]
   [28 20 11 11]]

  [[ 9  6 16  9]
   [27 30  9 10]
   [20 17  7 21]
   [24 20 16 16]]]

 [[[11  9 12 17]
   [17  1 15 19]
   [21 16 16 21]
   [20  7 15  6]]

  [[18 28 19  3]
   [ 1  6  6 15]
   [ 2 27  3  4]
   [16 22 12  6]]]]
(8, 8)
[[26. 11. 27.  9.  9.  6. 27. 30.]
 [22. 30. 21. 16. 16.  9.  9. 10.]
 [27.  3. 28. 20. 20. 17. 24. 20.]
 [28.  3. 11. 11.  7. 21. 16. 16.]
 [11.  9. 17.  1. 18. 28.  1.  6.]
 [12. 17. 15. 19. 19.  3.  6. 15.]
 [21. 16. 20.  7.  2. 27. 16. 22.]
 [16. 21. 15.  6.  3.  4. 12.  6.]]
```

我们通过im2col函数得到了二维的输入数据，同样我们也可以得到卷积核的二维数据，两个相乘，就会得到输出数据的二维数据

#### 输出数据

我们在上面得到了输出的二维数据，但是


### 池化层实现

```python
import numpy as np  
from im2ccol import im2col  
  
class Pooling:  
    def __init__(self, pool_h, pool_w, stride=1, pad=0):  
        self.pool_h = pool_h  
        self.pool_w = pool_w  
        self.stride = stride  
        self.pad = pad  
    def forward(self, x):  
        N, C, H, W = x.shape  
        out_h = int(1 + (H - self.pool_h) / self.stride)  
        out_w = int(1 + (W - self.pool_w) / self.stride)  
        #  
        col = im2col(x, self.pool_h, self.pool_w, self.stride, self.pad)  
        col = col.reshape(-1, self.pool_h*self.pool_w)  
        #  
        out = np.max(col, axis=1)  
        #   
out = out.reshape(N, out_h, out_w, C).transpose(0, 3, 1, 2)  
        return out
```


### 代表性的CNN

#### LeNet



#### AlexNet



